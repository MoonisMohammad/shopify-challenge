<p style="color: green"><%= notice %></p>

<h1>Inventory</h1>

<div id="items">
  <% @items.each do |item| %>
  <div id="<%= dom_id item %>">
    
      <strong>Name:</strong>
      <%= item.name %>
    
      <strong>| Category:</strong>
      <%= item.category %>
    
      <strong>| Quantity:</strong>
      <%= item.quantity %>
    
      <strong>|</strong>
      <%= link_to "Show this item", item %>
  <% end %>
</div>
<br>
<%= button_to "Export to CSV", "/items.csv",method: :get %>
<br>
<%= link_to "New item", new_item_path %>

